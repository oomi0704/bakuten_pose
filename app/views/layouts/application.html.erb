<!DOCTYPE html>
<html>
  <head>
    <title>BakutenPose</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- TensorFlow.jsライブラリ -->
    <script src="https://cdn.jsdelivr.net/npm/@tensorflow/tfjs-core"></script>
    <script src="https://cdn.jsdelivr.net/npm/@tensorflow/tfjs-converter"></script>
    <script src="https://cdn.jsdelivr.net/npm/@tensorflow/tfjs-backend-webgl"></script>
    <script src="https://cdn.jsdelivr.net/npm/@tensorflow-models/pose-detection"></script>
  </head>

  <body>
    <!-- ナビゲーションバー -->
    <nav class="bg-blue-600 shadow-lg">
      <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between items-center py-4">
          <div class="flex items-center">
            <h1 class="text-white text-xl font-bold">BakutenPose</h1>
          </div>
          
          <div class="flex items-center space-x-4">
            <% if current_user %>
              <!-- ログイン済みの場合 -->
              <span class="text-white">こんにちは、<%= current_user.email %>さん</span>
              <%= form_with url: logout_path, method: :delete, local: true, class: "inline" do |form| %>
                <%= form.submit "ログアウト", 
                    class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded transition duration-300 cursor-pointer" %>
              <% end %>
            <% else %>
              <!-- 未ログインの場合 -->
              <%= link_to "ログイン", login_path, 
                          class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded transition duration-300" %>
              <%= link_to "新規登録", signup_path, 
                          class: "bg-orange-500 hover:bg-orange-700 text-white font-bold py-2 px-4 rounded transition duration-300" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- フラッシュメッセージの表示 -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4 mx-4 mt-4" role="alert">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4 mx-4 mt-4" role="alert">
        <%= alert %>
      </div>
    <% end %>
    
    <main class="min-h-screen bg-gray-50">
      <%= yield %>
    </main>
  </body>
</html>
