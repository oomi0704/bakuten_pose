<h1>新しく写真を登録</h1>

<%= render "form", user: @user %>

<br>

<div>
  <%= link_to "Back to users", users_path, class: "large-link" %>
</div>

<style>
  body {
    font-size: 18px; /* 全体のフォントサイズを大きく */
  }

  h1 {
    font-size: 32px; /* 見出しをさらに大きく */
  }

  .large-link {
    font-size: 20px; /* リンクのフォントサイズを大きく */
    padding: 10px 20px; /* リンクのパディングを追加して大きく見せる */
    display: inline-block; /* インラインブロックにしてボタンのように表示 */
    background-color: #3498db; /* 青色の背景 */
    color: white; /* テキストの色を白に */
    text-decoration: none; /* 下線をなくす */
    border-radius: 5px; /* 角を丸くする */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* 影を追加 */
    transition: background-color 0.3s ease; /* 背景色の変化にトランジションを追加 */
  }

  .large-link:hover {
    background-color: #2980b9; /* ホバー時の背景色を変更 */
  }
</style>
